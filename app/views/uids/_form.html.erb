<%= form_for(@uid) do |f| %>
  <% if @uid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uid.errors.count, "error") %> prohibited this uid from being saved:</h2>

      <ul>
      <% @uid.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
      <%= f.label :user_id %><br>
     <%= f.collection_select(:user_id, User.where(:username => 'djensen'), :id, :username) %>
  </div>

  <div class="field">
    <%= f.label :uid %><br>
    <%= f.text_field :uid, :value => "#{@theuid.to_s}" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
